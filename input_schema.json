{
  "title": "Kuaishou TopN Downloader",
  "description": "Recherche Kuaishou, sélectionne les Top-N par popularité et enregistre les vidéos (MP4 si dispo).",
  "type": "object",
  "schemaVersion": 1,
  "additionalProperties": false,
  "properties": {
    "keyword": {
      "title": "Mot-clé de recherche",
      "type": "string",
      "editor": "textfield",
      "description": "Mot-clé à chercher sur Kuaishou. Laissez vide si vous fournissez des URLs directes."
    },
    "urlList": {
      "title": "URLs directes (optionnel)",
      "type": "array",
      "editor": "stringList",
      "items": { "type": "string" },
      "description": "Une liste d’URLs Kuaishou (ex. /short-video/... ou /f/...)."
    },
    "limit": {
      "title": "Nombre maximum de vidéos (Top-N)",
      "type": "integer",
      "editor": "number",
      "default": 3,
      "minimum": 1,
      "maximum": 50,
      "description": "Combien de vidéos traiter au maximum pour une recherche."
    },
    "useApifyProxy": {
      "title": "Utiliser Apify Proxy",
      "type": "boolean",
      "editor": "checkbox",
      "default": true,
      "description": "Active le proxy Apify pour réduire les blocages."
    },
    "apifyProxyGroups": {
      "title": "Groupes de proxy (optionnel)",
      "type": "array",
      "editor": "stringList",
      "items": { "type": "string" },
      "description": "Ex.: [\"RESIDENTIAL\"] pour proxys résidentiels (si disponible sur votre compte)."
    },
    "apifyProxyCountry": {
      "title": "Pays du proxy (optionnel)",
      "type": "string",
      "editor": "textfield",
      "description": "Code pays ISO-2 (ex.: HK, SG, US) si supporté par le groupe choisi."
    },
    "saveBinary": {
      "title": "Enregistrer les MP4 dans le KV",
      "type": "boolean",
      "editor": "checkbox",
      "default": true,
      "description": "Si vrai, sauvegarde le .mp4 dans le Key-Value Store quand une URL MP4 est trouvée."
    },
    "acceptM3U8": {
      "title": "Accepter les flux M3U8",
      "type": "boolean",
      "editor": "checkbox",
      "default": false,
      "description": "Si vrai, accepte aussi les URLs .m3u8 (HLS)."
    }
  },
  "required": []
}
