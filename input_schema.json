{
  "title": "Kuaishou TopN Downloader",
  "description": "Cherche les vidéos Kuaishou, sélectionne les Top-N par popularité et télécharge les MP4 (si disponibles).",
  "type": "object",
  "schemaVersion": 1,
  "additionalProperties": false,
  "properties": {
    "keyword": {
      "title": "Mot-clé de recherche",
      "type": "string",
      "description": "Mot-clé à chercher sur Kuaishou (laisser vide si vous fournissez des URLs directes)."
    },
    "urlList": {
      "title": "URLs directes de vidéos",
      "type": "array",
      "items": { "type": "string" },
      "description": "Liste optionnelle d'URLs Kuaishou (ex: /short-video/... ou /f/...)."
    },
    "limit": {
      "title": "Nombre maximum de vidéos",
      "type": "integer",
      "default": 3,
      "minimum": 1,
      "maximum": 50,
      "description": "Combien de vidéos traiter au maximum (pour la recherche Top-N)."
    },
    "useApifyProxy": {
      "title": "Utiliser Apify Proxy",
      "type": "boolean",
      "default": true,
      "description": "Active le proxy Apify pour réduire les blocages."
    },
    "apifyProxyGroups": {
      "title": "Groupes de proxy (optionnel)",
      "type": "array",
      "items": { "type": "string" },
      "description": "Ex.: [\"RESIDENTIAL\"] pour utiliser les proxys résidentiels."
    },
    "apifyProxyCountry": {
      "title": "Pays du proxy (optionnel)",
      "type": "string",
      "description": "Code pays ISO-2 (ex.: HK, SG, US) si disponible pour votre groupe proxy."
    },
    "saveBinary": {
      "title": "Enregistrer les MP4 dans le KV",
      "type": "boolean",
      "default": true,
      "description": "Si vrai, sauvegarde le MP4 dans le Key-Value Store quand une URL .mp4 est trouvée."
    },
    "acceptM3U8": {
      "title": "Accepter les flux m3u8",
      "type": "boolean",
      "default": false,
      "description": "Si vrai, accepte aussi les URLs .m3u8 (stream HLS)."
    }
  },
  "required": []
}
