{
  "schemaVersion": 1,
  "title": "Kuaishou Top-N Downloader",
  "type": "object",
  "properties": {
    "keyword": {
      "title": "Keyword (search)",
      "type": "string",
      "description": "Termes à chercher sur Kuaishou (optionnel si vous fournissez urlList)."
    },
    "urlList": {
      "title": "Video page URLs (optional)",
      "type": "array",
      "items": { "type": "string" },
      "description": "Liste d'URLs Kuaishou déjà trouvées (bypass la recherche)."
    },
    "limit": {
      "title": "How many videos to download",
      "type": "integer",
      "minimum": 1,
      "maximum": 50,
      "default": 3,
      "description": "Nombre de vidéos à télécharger (Top-N)."
    },
    "useApifyProxy": {
      "title": "Use Apify Proxy",
      "type": "boolean",
      "default": true
    },
    "saveBinary": {
      "title": "Save MP4 to Key-Value Store",
      "type": "boolean",
      "default": true,
      "description": "Si true, les MP4 sont stockés dans le KV (récupérables par API)."
    },
    "acceptM3U8": {
      "title": "Allow HLS (.m3u8) if MP4 not found (won't be downloaded)",
      "type": "boolean",
      "default": false,
      "description": "Accepte une URL .m3u8 si pas de MP4 (elle ne sera pas téléchargée)."
    }
  },
  "required": [],
  "additionalProperties": false
}
